# Endereço da API do TERCEIRO360 (usado apenas no servidor, nunca no navegador).
API_URL=http://localhost:8000

# Em produção, o endereço interno da API. Exemplo com os dois no mesmo Compose:
# API_URL=http://api:8000
